# 📊 АНАЛИЗ ПРОЕКТА SOUL AI BOT

**Дата анализа:** 21 января 2026  
**Готовность к запуску:** ~90%  
**Ожидаемая нагрузка:** ~100 пользователей одновременно

---

## 📁 СТРУКТУРА ПРОЕКТА

### Масштаб:
- **260 Python файлов** (handlers, utils, core, database)
- **19 handlers** (роутеров Telegram)
- **44 таблицы** в PostgreSQL
- **31 HTML шаблон** для WebApp
- **2 сервиса:** aibot (Telegram) + soul-bot-web (Flask)
- **7 пользователей** в тестовой БД
- **2 активные подписки**
- **11 платёжных транзакций**

### Технологический стек:
```
Backend:  Python 3.12, aiogram 3.4.1, asyncpg, asyncio
Database: PostgreSQL (44 таблицы), Redis (кэш)
API:      OpenRouter (Claude Sonnet 4.5), VseGPT (изображения/видео)
Payment:  Robokassa (тестовый режим)
WebApp:   Flask 3.0, Gunicorn, Nginx
Server:   Ubuntu, systemd
```

---

## 🔴 КРИТИЧЕСКИЕ СИСТЕМЫ

### 1. Оплата и подписки 💰
**Статус:** ⚠️ ТРЕБУЕТ ТЩАТЕЛЬНОЙ ПРОВЕРКИ

**Файлы:**
- `handlers/subscription.py` — логика подписок
- `utils/robokassa.py` — интеграция платёжного шлюза
- `web_app.py` → endpoints `/api/payment/create`, `/robokassa/result`
- `templates/payment.html` — WebApp оплаты

**Потенциальные риски:**
- Неправильная генерация signature → платежи не проходят
- Webhook не обрабатывается → деньги списаны, звёзды не зачислены
- Дубликаты транзакций → двойное списание
- Тестовый режим → нужно переключить на production

**Что проверить:**
- ✅ Создание платежа (все 3 тарифа + 2 пакета)
- ✅ Проверка подписи Robokassa
- ✅ Webhook обработка при успехе/неудаче
- ✅ Начисление звёзд и активация подписки
- ✅ Обработка ошибок и edge cases

---

### 2. Реферальная программа 🎁
**Статус:** ⚠️ ТРЕБУЕТ ПРОВЕРКИ

**Файлы:**
- `handlers/start.py` → обработка `/start ref_12345`
- `handlers/subscription.py` → начисление бонусов
- Таблица `referrals` в PostgreSQL

**Бонусы:**
- Mini: +1,000⭐ рефереру
- Standard: +2,000⭐ рефереру
- Premium: +4,000⭐ рефереру

**Потенциальные риски:**
- Нет проверки на self-referral → пользователь может пригласить себя
- Дубликаты рефералов → начисление дважды
- Несуществующий реферер → ошибка
- Уведомления не приходят → реферер не знает о новом пользователе

---

### 3. Парные сессии Silas 🤝
**Статус:** ✅ ИСПРАВЛЕНА КРИТИЧЕСКАЯ ОШИБКА

**Что было:**
- При переходе по deep link `/start pair_XXXX` новый пользователь НЕ создавался в БД
- Последующие попытки использования → "Ошибка регистрации"

**Что исправлено:**
- Теперь пользователь создаётся ДО показа парной сессии
- Deep link работает корректно

**Что ещё проверить:**
- Присоединение второго участника
- Чат работает для обоих
- Завершение/отмена сессии
- Истечение timeout
- Удаление старых сессий

---

### 4. Система звёзд ⭐
**Статус:** ⚠️ ТРЕБУЕТ ПРОВЕРКИ РАСЧЁТОВ

**Текущие цены:**
- Диалог Claude Sonnet: ~7⭐ за запрос
- Flux Schnell: 50⭐
- Flux 2: 120⭐
- Kontext Pro: 200⭐
- Kontext Max: 350⭐
- Upscale: 30⭐

**Формула:**
```
Цена в ⭐ = (API цена в ₽ × маржа 1.7) / 0.1225₽
```

**Потенциальные риски:**
- Неправильные расчёты → убытки или завышенные цены
- Баланс уходит в минус → пользователь "крадёт" звёзды
- Приоритет подписки vs бонусов — работает ли?

---

### 5. Стриминг ответов 💬
**Статус:** ⚠️ ВОЗМОЖНО ДУБЛИРОВАНИЕ СООБЩЕНИЙ

**Проблема:**
`stream_response()` возвращает `(text, sent_msg)`, где `sent_msg` — уже отправленное сообщение.
Если handler'ы не знают об этом → могут отправить дважды.

**Что проверить:**
- Все места где вызывается `stream_response()`
- Не дублируются ли сообщения
- Корректно ли редактируется sent_msg

---

## ⚠️ ИЗВЕСТНЫЕ ПРОБЛЕМЫ

### 1. Админ-панель (handlers/admin.py)
**Из .cursorrules:** "баги"  
**Статус:** Неизвестно, требует проверки

**Что проверить:**
- Статистика пользователей
- Рассылка сообщений
- Блокировка/разблокировка
- Изменение баланса
- WebApp админки

---

### 2. Redis аутентификация
**Warning в логах:** "Authentication required"  
**Влияние:** Пока неизвестно

**Что проверить:**
- Работает ли кэш настроек?
- Теряются ли данные?
- Нужен ли пароль для Redis?

---

### 3. Устаревший SQLite (database/db.py)
**Статус:** НЕ ИСПОЛЬЗУЕТСЯ  
**Действие:** Убедиться что нигде не импортируется, можно удалить

---

## 🎯 КРИТИЧЕСКИЕ ТОЧКИ ДЛЯ ПРОВЕРКИ

### Handlers (все 19):
1. ✅ start.py — регистрация (deep link ИСПРАВЛЕН)
2. 🔴 subscription.py — оплата (КРИТИЧНО)
3. 🔴 silas/handler.py — парные сессии (КРИТИЧНО)
4. ⚠️ admin.py — админка (баги)
5. ⚠️ luca/handler.py — главный чат
6. ⚠️ images.py — генерация изображений
7. ⚠️ lifestyle/magic.py — магия
8. ⚠️ titus/handler.py — обучение (нет голоса)
9. ⚠️ health.py — здоровье
10. ⚠️ socials.py — соцсети
11. ⚠️ voice.py — голосовые (только Luca/Silas)
12. И остальные...

### Утилиты:
- 🔴 utils/robokassa.py — платежи
- 🔴 utils/streaming.py — стриминг
- 🔴 utils/openrouter.py — API
- ⚠️ utils/voice.py — TTS/STT

### WebApp:
- 🔴 payment.html — оплата
- ⚠️ webapp.html — кабинет
- ⚠️ silas_pair_*.html — парные сессии
- ⚠️ Остальные шаблоны

---

## 🧪 ПЛАН ТЕСТИРОВАНИЯ

### Этап 1: Критические системы
1. Регистрация (все варианты)
2. Оплата Robokassa (все тарифы)
3. Рефералы (все бонусы)
4. Парные сессии (полный цикл)
5. Баланс звёзд (списание/начисление)

### Этап 2: Основной функционал
6. Luca (чат + голос + память)
7. Silas (психолог + голос)
8. Titus (обучение, текст)
9. Генерация изображений
10. Lifestyle (все 6 подмодулей)

### Этап 3: Нагрузка
11. 100 одновременных /start
12. 50 параллельных диалогов
13. 20 одновременных генераций
14. 10 параллельных платежей

---

## 📊 ТЕКУЩЕЕ СОСТОЯНИЕ

### ✅ Работает:
- PostgreSQL подключение
- Redis подключение
- Базовая регистрация
- Принятие соглашения
- Начисление бонуса 250⭐
- Бот запущен и отвечает

### ❓ Требует проверки:
- Оплата Robokassa
- Реферальная программа
- Парные сессии (логика)
- Админ-панель
- Все handlers
- WebApp страницы
- Нагрузка 100+ пользователей

### ✅ Исправлено:
- Deep link парных сессий (создание пользователя)

---

## 🚨 РИСКИ ПРИ ЗАПУСКЕ

### Высокий риск:
1. **Платежи не работают** → пользователи не могут купить
2. **Webhook не обрабатывается** → деньги списаны, звёзды не зачислены
3. **Рефералы не работают** → обещанные бонусы не приходят
4. **БД падает под нагрузкой** → бот недоступен

### Средний риск:
5. **API OpenRouter timeout** → долгие ответы или ошибки
6. **VseGPT rate limit** → генерация изображений недоступна
7. **Memory leaks** → бот крашится через несколько часов
8. **Неправильные цены** → убытки или завышенная стоимость

### Низкий риск:
9. **Мелкие баги в UI** → плохой UX, но работает
10. **Redis проблемы** → настройки не сохраняются
11. **Логи переполняются** → диск заполнен

---

## 💡 РЕКОМЕНДАЦИИ

### Перед запуском (ОБЯЗАТЕЛЬНО):
1. ✅ Полная диагностика всех handlers
2. ✅ Тест всех критических систем
3. ✅ Нагрузочное тестирование 100+ пользователей
4. ⚠️ Переключить Robokassa в production режим
5. ✅ Настроить мониторинг и алерты
6. ✅ Резервное копирование БД

### Мониторинг после запуска:
1. Логи в реальном времени: `journalctl -u aibot -f`
2. Статус сервисов: `systemctl status aibot soul-bot-web`
3. Нагрузка сервера: `htop`
4. БД подключения: `SELECT count(*) FROM pg_stat_activity`
5. Errors в логах: `journalctl -u aibot | grep -i error`

---

## 📝 ВЫВОДЫ

### Текущий статус: ⚠️ ТРЕБУЕТ ПОЛНОЙ ДИАГНОСТИКИ

**Что работает:**
- ✅ Инфраструктура (БД, Redis, сервисы)
- ✅ Базовая регистрация
- ✅ Бот запущен и отвечает

**Что неизвестно:**
- ❓ Оплата Robokassa (критично!)
- ❓ Реферальная программа
- ❓ Все handlers работают корректно?
- ❓ Выдержит ли 100 пользователей?

**Что нужно:**
1. Полная диагностика от Opus 4.5
2. Исправление всех найденных багов
3. Тестирование критических систем
4. Нагрузочное тестирование
5. Финальный smoke test

---

**Готовность к запуску:** ⚠️ НЕ ГОТОВ без полной диагностики

**Рекомендация:** Запустить полную диагностику Opus 4.5 ПЕРЕД запуском проекта.

---

**Подготовлено:** Claude Sonnet 4.5  
**Дата:** 2026-01-21  
**Следующий шаг:** Передать проект Opus 4.5 для полной диагностики
